cars = []
loop = True
while loop == True:
  ask = str(input("enter a car brand or press x to exit"))
  if ask == "x":
    print("exiting...")
    loop = False
  if ask != "x":
     ask = str(input("enter a car brand or press x to exit"))
